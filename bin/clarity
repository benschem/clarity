#!/usr/bin/env ruby
#
# This comment tells the OS how to execute the file when run directly from the terminal
# Uses the Ruby in `$PATH`

# Adds app library code to `$PATH` so I can use `require`
$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

require_relative '../app'
